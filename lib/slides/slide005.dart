import 'package:flutter/material.dart';

class Slide005 extends StatelessWidget {
  const Slide005({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "The project - Lambrecht Trailers",
        style: TextStyle(
          fontFamily: 'Nunito',
          fontSize: 80,
          color: Colors.white,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}
