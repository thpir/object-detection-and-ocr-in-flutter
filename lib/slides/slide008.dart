import 'package:flutter/material.dart';

class Slide008 extends StatelessWidget {
  const Slide008({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset(
        "assets/images/tag_reader_detail.png",
        fit: BoxFit.contain,
      )
    );
  }
}
