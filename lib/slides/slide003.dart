import 'package:flutter/material.dart';

class Slide003 extends StatelessWidget {
  const Slide003({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Cyber3Lab - who are we?",
        style: TextStyle(
          fontFamily: 'Nunito',
          fontSize: 80,
          color: Colors.white,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}
